# Copy to frontend/.env (git-ignored). Vite only exposes variables starting with VITE_.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Where the Express backend runs (same phone in dev):
VITE_API_URL=http://localhost:4000
